#include <stdio.h>

typedef struct {
    char nome;
    int matricula;
    int notas;
} Alunos;

int main() {

    Alunos aluno[5];
    int i;

    for (i = 0; i < 3; i++)
    {
        scanf("%s", aluno[i].nome);
        scanf("%d", &aluno[i].matricula);
        scanf("%d", &);
    }

   
    return 0;
}
