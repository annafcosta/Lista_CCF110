#include <stdio.h>
#include <math.h>

int main() {
        int num, i, cubo;

        printf("Digite o valor de 400 números: ");
        scanf("%d", &num);

        for (i = 1; i <= 400; i++) {
            cubo = pow();
        }

        printf("%d\n", cubo);

        return 0;
}


